@model GestionInmobiliaria.BusinessEntity.UnidadInmobiliaria

@{
    ViewBag.Title = "Delete";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<fieldset>
    <legend>Unidad inmobiliaria</legend>

    @Html.HiddenFor(model => model.id)

    <div class="display-label">N° Inmueble</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.NroInmuebleActual)
    </div>

    <div class="display-label">Direccion</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.Direccion)
    </div>

    <div class="display-label">Observaciones</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.Observaciones)
    </div>

    <div class="display-label">Area de terreno</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.AreaTerreno)
    </div>

    <div class="display-label"># de dormitorios</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.NumDormitorio)
    </div>

    <div class="display-label"># de baños</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.NumBanios)
    </div>

    <div class="display-label">Precio de venta</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.PrecioVenta)
    </div>
</fieldset>

@using (Html.BeginForm()) {
    <p>
        <input type="submit" value="Delete" /> |
        @Html.ActionLink("Back to List", "Index")
    </p>
}